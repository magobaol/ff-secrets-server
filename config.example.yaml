# Server runtime config (flat key: value). Copy to config.yaml on the host; not committed.
driver: 1password-connect-server
# Connect Server endpoint on the LAN — set the real host:port in the host's config.yaml.
endpoint: http://CONNECT_HOST:8080
registry: /app/runtime/registry.yaml
bearer-type: file
bearer-path: /app/runtime/bearer
